version: "2.16"

services:
  item-db: 
    image: mongo:3
    volumes:
      - app-db:/data/db
    restart: always
  item-app:
    image: yusuffranklin/item-app:v1
    ports:
      - 80:8080
    restart: always
volumes:
  app-db:
