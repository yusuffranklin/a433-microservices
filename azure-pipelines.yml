trigger:
- master

resources:
- repo: self

variables:
  #tag: '$(Build.BuildId)'
  tag: 'latest'

stages:
# Lint Dockerfile
- stage: Lint_Dockerfile
  displayName: Lint_Dockerfile
  jobs:
  - job: Lint_Dockerfile
    displayName: Lint_Dockerfile
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: DockerInstaller@0
      inputs:
        dockerVersion: '17.09.0-ce'
    - task: CmdLine@2
      inputs:
        script: 'docker run --rm -i hadolint/hadolint < Dockerfile'
        workingDirectory: '$(Build.SourcesDirectory)'
# Test
- stage: Test
  displayName: Test
  jobs:
  - job: Test
    displayName: Test
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: GoTool@0
      inputs:
        version: '1.15'
    - task: Go@0
      inputs:
        command: 'test'
        arguments: '-v'
        workingDirectory: '$(Build.SourcesDirectory)/cmd/web'
# Build and Push Docker Image  
- stage: Build_Image
  displayName: Build Image
  jobs:
  - job: Build_Image
    displayName: Build Image
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: Bash@3
      inputs:
        filePath: './build_push_image_karsajobs.sh'
        workingDirectory: '$(Build.SourcesDirectory)'
